class Solution {
    public int countOdds(int low, int high) {
        int count=0;
        int dif= high-low;
        if (low%2==0 && high%2==0){
            count+=dif/2;
        }
        else
            count+=dif/2 + 1;
        return count;

    }
